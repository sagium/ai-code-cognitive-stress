{
  "_comment": "Runtime configuration for ai-code-cognitive-stress. Edit these defaults to change behaviour without touching code.",
  "_comment_work_window": "The work window is inferred per-user from interaction timestamps by default (p10–p90 of your daytime message hours, requiring at least 5 distinct weekdays of data). To PIN a manual window instead, add a 'work_window' block with 'start' and 'end' (HH:MM, local time) and it will override inference. Example: {\"start\": \"09:00\", \"end\": \"19:00\"}.",
  "codl": {
    "_comment": "Engagement weighting for CODL. A session counts at full weight (1.0) only while you're actively driving it — within foreground_grace_minutes of one of YOUR messages. The rest of the time it is alive but 'cooking' in the background and counts at background_weight. Research basis: a background locus is the 'out-of-the-loop' supervisory regime, where monitoring is effortful and stressful (Warm et al. 2008; Endsley & Kiris 1995; Parasuraman & Manzey 2010) — discounted but never free. The magnitude (0.25) is borrowed from the prospective-memory monitoring cost of ~15-20% of ongoing-task capacity (Smith 2003; Masicampo & Baumeister 2011), at the conservative top of that range.",
    "foreground_grace_minutes": 5,
    "background_weight": 0.25
  },
  "closure": {
    "_comment": "Real closure-event ingestion for the Closure Deficit axis. 'repos' is an OPT-IN list of local git repo paths (absolute). When non-empty, commits/merges in those repos within the work window are counted as closed loops and the deficit becomes 1 - closures/loops_opened (loops_opened = streams started in the window). The tool never auto-discovers repos — it only scans what you list here (local git only, read-only, nothing leaves the machine). Leave empty to fall back to the legacy concurrency-presence proxy. Research basis: an open loop keeps consuming working memory until closed (Masicampo & Baumeister 2011); closure removes attention residue (Leroy 2009) and is a recovery resource (Sonnentag & Fritz 2007); demands without recoverable resources drive burnout (Demerouti et al. 2001).",
    "repos": []
  }
}
